<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.databinderdemo1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoActivity8"></activity>
        <activity android:name=".DemoActivity7" />
        <activity android:name=".DemoActivity6" />
        <activity android:name=".DemoActivity5" />
        <activity android:name=".DemoActivity4" />
        <activity android:name=".DemoActivity3" />
        <activity android:name=".DemoActivity2" />
        <activity android:name=".DemoActivity1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>